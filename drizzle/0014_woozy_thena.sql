-- The schema objects in this generated migration are already created by
-- 0013_ep_sub_merchant_form.sql. Keep this migration as a no-op so Drizzle's
-- journal can advance without attempting to recreate existing tables/types.
SELECT 1;
